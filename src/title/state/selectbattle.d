module title.state.selectbattle;

import dau.all;
import model.all;
import title.title;

/// player may click on a unit to issue orders
class SelectBattle : State!Title {
  this() {
  }

  override {
    void enter(Title title) {
    }

    void update(Title title, float time, InputManager input) {
    }
  }
}
